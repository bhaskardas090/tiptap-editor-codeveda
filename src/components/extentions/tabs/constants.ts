/** Smallest number of tab items a tabs node can hold (schema requires one). */
export const MIN_TABS = 1;

/** Largest number of tab items a tabs node can hold. Keep in sync with
 *  `$max-tabs` in tabs.scss, which generates the active-tab visibility rules. */
export const MAX_TABS = 6;
