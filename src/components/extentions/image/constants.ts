/** Placement of an image within its column. */
export type ImageAlign = "left" | "center" | "right";

/** Smallest share of the column an image may be dragged down to. */
export const MIN_IMAGE_WIDTH_PERCENT = 10;
